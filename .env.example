# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=your_mongo_username
MONGO_INITDB_ROOT_PASSWORD=your_secure_password
MONGO_INITDB_DATABASE=cafe_pos

# Backend Configuration
MONGODB_URI=mongodb://your_mongo_username:your_secure_password@mongodb:27017
MONGODB_DATABASE=cafe_pos
JWT_SECRET=your_jwt_secret_key_change_this
PORT=8080

# Frontend Configuration
VITE_API_URL=http://localhost:8080/api

# Production Notes:
# 1. Change all default passwords
# 2. Use strong JWT secret (min 32 characters)
# 3. Use environment-specific MongoDB credentials
# 4. Never commit actual .env file to git
